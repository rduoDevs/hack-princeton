// Legacy file — superseded by chatDiscriminator.ts (MODE 2) and cotHybridAnalyzer.ts (MODE 3).
// Kept as a re-export shim so nothing breaks if it's imported elsewhere.
export { ChatDiscriminator as Discriminator } from './chatDiscriminator'
